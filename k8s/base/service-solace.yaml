apiVersion: v1
kind: Service
metadata:
  name: solace-broker
  namespace: verdictcouncil
spec:
  type: ClusterIP
  selector:
    app: solace-broker
  ports:
    - name: smf
      port: 55555
      targetPort: 55555
    - name: semp
      port: 8080
      targetPort: 8080
    - name: health
      port: 5550
      targetPort: 5550
