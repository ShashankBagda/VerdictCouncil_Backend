apiVersion: apps/v1
kind: Deployment
metadata:
  name: layer2-aggregator
  namespace: verdictcouncil
  labels:
    app: verdictcouncil
    component: layer2-aggregator
spec:
  replicas: 1
  selector:
    matchLabels:
      app: verdictcouncil
      component: layer2-aggregator
  template:
    metadata:
      labels:
        app: verdictcouncil
        component: layer2-aggregator
    spec:
      containers:
        - name: layer2-aggregator
          image: verdictcouncil:latest
          args: ["--config", "/app/configs/services/layer2-aggregator.yaml"]
          envFrom:
            - secretRef:
                name: verdictcouncil-secrets
          resources:
            requests:
              cpu: 250m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            exec:
              command: ["python", "-c", "import sys; sys.exit(0)"]
            periodSeconds: 30
