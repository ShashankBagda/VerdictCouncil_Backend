apiVersion: apps/v1
kind: Deployment
metadata:
  name: argument-construction
  namespace: verdictcouncil
  labels:
    app: verdictcouncil
    component: argument-construction
spec:
  replicas: 1
  selector:
    matchLabels:
      app: verdictcouncil
      component: argument-construction
  template:
    metadata:
      labels:
        app: verdictcouncil
        component: argument-construction
    spec:
      containers:
        - name: argument-construction
          image: verdictcouncil:latest
          args: ["--config", "/app/configs/agents/argument-construction.yaml"]
          envFrom:
            - secretRef:
                name: verdictcouncil-secrets
          resources:
            requests:
              cpu: 250m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            exec:
              command: ["python", "-c", "import sys; sys.exit(0)"]
            periodSeconds: 30
