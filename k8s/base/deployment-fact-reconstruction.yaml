apiVersion: apps/v1
kind: Deployment
metadata:
  name: fact-reconstruction
  namespace: verdictcouncil
  labels:
    app: verdictcouncil
    component: fact-reconstruction
spec:
  replicas: 1
  selector:
    matchLabels:
      app: verdictcouncil
      component: fact-reconstruction
  template:
    metadata:
      labels:
        app: verdictcouncil
        component: fact-reconstruction
    spec:
      containers:
        - name: fact-reconstruction
          image: verdictcouncil:latest
          args: ["--config", "/app/configs/agents/fact-reconstruction.yaml"]
          envFrom:
            - secretRef:
                name: verdictcouncil-secrets
          resources:
            requests:
              cpu: 250m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            exec:
              command: ["python", "-c", "import sys; sys.exit(0)"]
            periodSeconds: 30
