apiVersion: v1
kind: Service
metadata:
  name: web-gateway
  namespace: verdictcouncil
spec:
  type: ClusterIP
  selector:
    app: verdictcouncil
    component: web-gateway
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
