apiVersion: apps/v1
kind: Deployment
metadata:
  name: complexity-routing
  namespace: verdictcouncil
  labels:
    app: verdictcouncil
    component: complexity-routing
spec:
  replicas: 1
  selector:
    matchLabels:
      app: verdictcouncil
      component: complexity-routing
  template:
    metadata:
      labels:
        app: verdictcouncil
        component: complexity-routing
    spec:
      containers:
        - name: complexity-routing
          image: verdictcouncil:latest
          args: ["--config", "/app/configs/agents/complexity-routing.yaml"]
          envFrom:
            - secretRef:
                name: verdictcouncil-secrets
          resources:
            requests:
              cpu: 250m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            exec:
              command: ["python", "-c", "import sys; sys.exit(0)"]
            periodSeconds: 30
