apiVersion: v1
kind: Service
metadata:
  name: api-service
  namespace: verdictcouncil
  labels:
    app: verdictcouncil
    component: api-service
spec:
  type: ClusterIP
  selector:
    app: verdictcouncil
    component: api-service
  ports:
    - name: http
      port: 8001
      targetPort: 8001
      protocol: TCP
