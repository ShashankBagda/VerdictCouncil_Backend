apiVersion: v1
kind: Service
metadata:
  name: api-service
  namespace: verdictcouncil
spec:
  type: ClusterIP
  selector:
    app: verdictcouncil
    component: api-service
  ports:
    - port: 8001
      targetPort: 8001
      protocol: TCP
