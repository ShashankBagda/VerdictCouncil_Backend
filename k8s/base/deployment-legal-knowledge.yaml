apiVersion: apps/v1
kind: Deployment
metadata:
  name: legal-knowledge
  namespace: verdictcouncil
  labels:
    app: verdictcouncil
    component: legal-knowledge
spec:
  replicas: 1
  selector:
    matchLabels:
      app: verdictcouncil
      component: legal-knowledge
  template:
    metadata:
      labels:
        app: verdictcouncil
        component: legal-knowledge
    spec:
      containers:
        - name: legal-knowledge
          image: verdictcouncil:latest
          args: ["--config", "/app/configs/agents/legal-knowledge.yaml"]
          envFrom:
            - secretRef:
                name: verdictcouncil-secrets
          resources:
            requests:
              cpu: 250m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            exec:
              command: ["python", "-c", "import sys; sys.exit(0)"]
            periodSeconds: 30
